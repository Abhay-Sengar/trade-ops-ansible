---
# Bind on all interfaces so the VirtualBox NAT port-forward can reach it
monitoring_netdata_bind: "0.0.0.0:19999"
# Static build keeps everything under /opt/netdata
monitoring_netdata_conf: "/opt/netdata/etc/netdata/netdata.conf"
